# Private. You don't need to touch this.
# If contributing, these are useful for removing global scope and reducing conflicts.
# This is a *procedural*, modular CLI package, not an object-oriented one.
progress = 0
suffix = '%'
sfx_len = len(suffix)
to = 100
